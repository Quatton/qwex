tasks:
  helper1:
    cmd: echo "Helper 1"

  helper2:
    cmd: echo "Helper 2"

  main:
    cmd: |
      # Declare multiple functions
      {% declare tasks.helper1, tasks.helper2 %}
      # Now call them
      helper1
      helper2
