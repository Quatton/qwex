tasks:
  helper:
    cmd: |
      echo "I am helper"

  main:
    vars:
      task_to_run: "{{ tasks.helper }}"
    cmd: |
      docker run python:3.12 -it --rm -- "{% context %}
        {{ vars.task_to_run }}
      {% endcontext %}"
