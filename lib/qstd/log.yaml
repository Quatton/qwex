name: log

modules:
  utils:
    source: utils.yaml

tasks:
  debug:
    run: |
      if [ "${DEBUG:-0}" -eq 0 ]; then
        return
      fi
      {{ utils.once }} "utils:color" "{{ utils.color }}"
      echo -e "$1" >&2
