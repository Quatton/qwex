name: steps

tasks:
  compose:
    run: |
      echo "Running step: $1"
      eval "$2"

  steps:
    run: |
      echo "Running multiple steps from: $1"
      # This would be parsed and executed, but for now just a stub
      echo "Steps not fully implemented yet"
