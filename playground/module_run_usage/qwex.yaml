name: module_run_usage

modules:
  log:
    source: ../../lib/qstd/log.yaml

tasks:
  greet:
    run: echo "Hello from module_run_usage"
  
  debug_message:
    run: |
      {{ log.tasks.debug }} "Debug message"
  
  combined:
    run: |
      echo "Step 1: Greeting"
      {{ log.tasks.debug }} "About to say debug"
      echo "Step 2: Done"
