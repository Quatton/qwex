name: task-with-args

tasks:
  greet:
    run: |
      local name="$1"
      local greeting="$2"
      echo "${greeting}, ${name}!"

  repeat:
    run: |
      local text="$1"
      local times="$2"
      for i in $(seq 1 "$times"); do
        echo "$text"
      done
