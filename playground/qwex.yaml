modules:
  uvworkspace:
    uses: ./uv-workspace/qwex.yaml
  lifecycle:
    uses: ./lifecycle/qwex.yaml

tasks:
  clean:
    cmd: |
      {{ modules["uvworkspace"].tasks.clean }}
      {{ modules["lifecycle"].tasks.clean }}
  all:
    cmd: |
      {{ modules["uvworkspace"].tasks.all }}
      {{ modules["lifecycle"].tasks.all }}
  test:
    cmd: |
      {{ modules["uvworkspace"].tasks.test }}  
      {{ modules["lifecycle"].tasks.test }}
