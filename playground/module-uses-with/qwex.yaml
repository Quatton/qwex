name: module-uses-with
modules:
  steps:
    source: std/steps

tasks:
  composite:
    uses: steps.compose
    with:
      - name: "Step 1"
        run: "echo First step"
  
  multi_step:
    uses: steps.compose
    with:
      - name: "Build"
        run: "echo Building..."
      - name: "Test"
        run: "echo Testing..."
      - name: "Deploy"
        run: "echo Deploying..."
