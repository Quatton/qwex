name: module-uses-with
modules:
  utils:
    uses: std/utils

tasks:
  composite:
    run: |
      {{ utils.once }} "utils:color" "{{ utils.color }}"
      echo "First"

  multi_step:
    run: |
      {{ utils.once }} "utils:color" "{{ utils.color }}"
      echo "Building..."
