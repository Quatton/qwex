# Copy this file to `.env` at the repo root and fill in the values.
# The `.env` file is git-ignored; keep it private.

# Required: reusable Tailscale auth key that allows advertising the cluster podCIDR routes.
# Example: tskey-auth-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
K3S_VPN_AUTH_KEY=

# Optional: shared k3s token. If left empty the server will generate one and you can
# retrieve it later with `just lima-secret`.
K3S_TOKEN=

# Optional defaults for worker nodes. Agents still need the server's Tailscale address
# exported at runtime, but you can keep a known hostname here if you use MagicDNS.
#K3S_SERVER_TAILSCALE_IP=
