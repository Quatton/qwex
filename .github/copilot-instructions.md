use command "act" (alias for source .venv/bin/activate.fish) to activate the virtual environment.

silence the vscode problems before calling a task done.

i use fish shell, so the commands may differ for bash/zsh.
especially please don't use here doc :(
